summary: a packrat parser generator for C
git-url: https://github.com/enechaev/packcc.git

install: |
    run "$CC $CPPFLAGS $CFLAGS $LDFLAGS -o packcc $SOURCE_DIR/packcc.c" &&
    install_bins packcc
